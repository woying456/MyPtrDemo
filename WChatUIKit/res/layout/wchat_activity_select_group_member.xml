<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.android.gmacs.view.PinnedHeaderListView
        android:id="@+id/pinnedheaderlistview_contacts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/search_container"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@null"
        android:dividerHeight="0dip"
        android:listSelector="@android:color/transparent"
        android:scrollbars="none" />

    <TextView
        android:id="@+id/tv_toast_index"
        android:layout_width="63dip"
        android:layout_height="63dip"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:gravity="center"
        android:paddingBottom="4dip"
        android:textColor="#FF8700"
        android:textSize="48sp"
        android:visibility="gone" />

    <com.android.gmacs.view.FastLetterIndexView
        android:id="@+id/fastLetterIndexView"
        android:layout_width="20dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:padding="5dp" />

    <TextView
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/search_container"
        android:gravity="center"
        android:textColor="#555555"
        android:textSize="24dp" />
</RelativeLayout>